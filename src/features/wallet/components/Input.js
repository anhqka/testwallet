import styled from 'styled-components';

export const Input = styled.input.attrs(() => ({ inputMode:'none'}))`
    background: none;
    outline: none;
    font-size: 16px;
    color: #19D6FF;
    border: none;
`;